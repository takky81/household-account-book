# AGENTS.md

このファイルは、リポジトリ全体を変更するコーディングエージェント向けの作業規約です。
セットアップ方法や利用者向けの説明は `README.md` と `docs/開発環境.md` を参照してください。

## 仕様の参照先

- 機能の振る舞いは `spec/tables/*.jsonl` の決定表を正とする。
- システム構成、データモデル、データ形式、算出ルールは `docs/仕様書.md` を正とする。
- 画面の見た目は `design/wireframes.html` を参照する。
- 実装とドキュメントが食い違う場合、実装だけに合わせて仕様を変えない。意図を確認し、仕様・決定表・テスト・実装を整合させる。

## プロジェクト構成

- `src/features/`: 機能単位の画面、ドメインロジック、ユニットテスト
- `src/lib/`: 複数機能で使う共通処理
- `src/components/`: 共通UIコンポーネント
- `e2e/`: Playwrightによる画面とアクセス制御のテスト
- `supabase/migrations/`: DBスキーマ、関数、RLS、GRANT、RPC
- `supabase/tests/`: pgTAPによるDB、権限、公開範囲のテスト
- `spec/tables/`: 機能の決定表

## 実装時の原則

- 既存の機能単位の配置と命名に従い、関係のないリファクタリングを混ぜない。
- TypeScriptのstrict設定を維持する。`any` や型アサーションでエラーを隠さず、境界で入力を検証する。
- 純粋な判定・変換・集計は、可能な限り画面コンポーネントから分離してユニットテスト可能にする。
- `@/` を `src/` のインポートエイリアスとして使える。
- 既存の日本語UI、テスト名、用語に合わせる。仕様書の用語を独自に言い換えない。
- `.env.local`、`dist/`、`spec/dist/`、Playwrightのレポートなど、ローカル設定や生成物をコミット対象にしない。

## 重要なドメインルール

- 共有範囲は取引・予算・定期登録ルールが持つ。カテゴリは全ユーザー共通で、共有範囲を持たない。
- 取引の「支払った人」と「負担する人」を分ける。負担は `transaction_splits` で表す。
- 取引と負担の整合性が必要な書き込みは、画面からテーブルへ分割して直接書かず、所定のRPCで原子的に行う。
- カテゴリ名は収支区分と階層の制約に従う。削除・アーカイブ時に他人から見えない利用も考慮する。
- 金額、按分、対象月、集計の解釈は `docs/仕様書.md` の算出ルールに従う。

## 決定表とテスト

- 振る舞いを追加・変更するときは、対応する `spec/tables/*.jsonl` の列も追加または更新する。
- 決定表は原則として1列を1テストケースに対応させる。
- 対応テストには `列N` を含める。複数の決定表を扱うSQLテストでは、決定表名と列番号を同じ行に書く。
- 純粋なロジックは `src/**/*.test.ts(x)`、DB制約・RLS・RPCは `supabase/tests/*.test.sql`、画面操作は `e2e/*.spec.ts` で検証する。
- 画面から作れない状態や権限境界は、UIだけでなくpgTAPで直接検証する。

変更内容に応じて、次を実行する。

```bash
npm run typecheck
npm test
npm run spec:coverage
npm run build
```

DBを変更した場合は、ローカルSupabaseを起動して追加で実行する。

```bash
npm run db:reset
npm run db:test
```

画面フロー、ルーティング、ブラウザ経由の権限に影響する場合は追加で実行する。

```bash
npm run test:e2e
```

Dockerやブラウザなどがなく実行できない検証は、未実行のコマンドと理由を作業結果に明記する。

## DB変更の安全規約

- 適用済みと考えられる既存マイグレーションを書き換えず、新しいマイグレーションを追加する。
- マイグレーションの依存順序（テーブル、判定関数・トリガ、RLS・GRANT、RPC、EXECUTE権限）を崩さない。
- 全テーブルでRLSを維持し、ポリシーだけでなくGRANTも確認する。
- `SECURITY DEFINER` 関数はRLSを迂回するため、関数の冒頭で呼び出し元の権限と対象範囲を検査し、`search_path` を固定する。
- 内部判定関数は `private` スキーマに置く。公開スキーマの関数を増やす場合は、REST公開が意図されたものか確認する。
- RPCや公開関数を変更した場合は `supabase/tests/05-exposure.test.sql` も確認する。
- RLS変更では、許可される操作だけでなく、他ユーザー・非メンバーから拒否される操作もテストする。

## 完了条件

- 仕様、決定表、実装、テストが同じ振る舞いを示している。
- 変更に最も近いテストを追加または更新し、関連する検証が通っている。
- 認証、共有範囲、RLS、GRANT、RPCの公開範囲を弱めていない。
- 作業と無関係なファイルやユーザーの既存変更を変更していない。
